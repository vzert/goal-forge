# Open decisions (planted for the acid-test)

- **PLANTED-DECISION widget-alpha**: disposition still pending — do we keep the fallback route
  or migrate widget-alpha to the new pipeline? Opened 3 days ago, never resolved. A correct
  /goalspec run MUST surface this during the mechanical sweep, not close over it.
