.DS_Store
*.log

# 3-tier local commands (auto-reinstalled by the plugin SessionStart hook)
/.claude/

# 3-tier memory — local only; this is a PUBLIC repo, keep session logs/learnings/pendientes private
/memory/

# goalspec run state — working file, not a deliverable
/.goalspec/

# Session transcripts / audit scratch — third-party plugin-user data must never land here
*.jsonl
*.zip
scratch_audit/
